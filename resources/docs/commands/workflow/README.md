# workflow

Inspect the bundled workflow notes that document repeatable retrieval tasks.

Examples:

```powershell
arxiv workflow list --format text
arxiv workflow show graph
arxiv workflow show B_search_and_rank
```
