# B_search_and_rank

Goal: search the catalog and rank candidate papers for follow-up reading.

Recommended sequence:

- start with `arxiv find papers ...` for broad discovery
- switch to `arxiv search query ...` when fielded filters are needed
- use `--format text` for quick terminal review
- keep JSON output when another tool will consume the results
