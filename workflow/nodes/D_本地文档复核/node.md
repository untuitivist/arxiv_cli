# D 本地文档复核

## Goal

Inspect bundled command documentation without leaving the terminal.

## Commands

```powershell
arxiv docs list
arxiv docs show search/query
arxiv docs show paper/get
```

## Success Criteria

- command tree is discoverable locally
- each documented node resolves to a readable `README.md`
